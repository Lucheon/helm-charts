apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pterodactyl-panel.fullname" . }}-mariadb-env
  labels:
    {{- include "pterodactyl-panel.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $key, $val := .Values.mariadb.secureEnv }}
  {{ $key }}: {{ $val | b64enc | quote }}
{{ end }}
